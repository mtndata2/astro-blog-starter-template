---
let latest = {};
try {
  const res = await fetch("https://astro-blog-starter-template.mtndatasales.workers.dev/api/latest-rates");
  if (res.ok) {
    const allRates = await res.json();
    latest = allRates.USD_NGN ?? {};
  }
} catch {}
---
<h1>Today's Rate Test</h1>
<pre>{JSON.stringify(latest, null, 2)}</pre>
<div>Buy: {latest.buy ?? "N/A"}</div>
<div>Sell: {latest.sell ?? "N/A"}</div>
